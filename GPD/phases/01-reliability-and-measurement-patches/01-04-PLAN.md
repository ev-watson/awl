---
phase: 01-reliability-and-measurement-patches
plan: 04
type: execute
wave: 3
depends_on: ["01-01", "01-02", "01-03"]
files_modified: []
interactive: true

conventions:
  units: "provider_reported_tokens_split_input_output"
  failure_categories: "format,schema,preflight,verify,timeout,network,model,unknown"
  result_schema: "snake_case_json_csv_fields"
  git_workflow: "branch -> PR -> CI -> merge (never push to main)"
  ci_evidence: "command_exit_status_date_branch_commit_and_ci_check_name"

contract:
  schema_version: 1
  scope:
    question: "Do all Phase 1 reliability patches pass local and CI verification without weakening existing gates?"
    in_scope:
      - "Run full local verification suite (cargo fmt, cargo test, cargo clippy) on the combined patch set (VAL-01)"
      - "Run Python fixture tests for tally.py and dispatch_cost_report.py"
      - "Audit for regressions: no tests removed, no lint suppressions added, no CI checks weakened"
      - "Record CI evidence with command exit status, date, branch, commit hash"
    out_of_scope:
      - "Implementing new patches (completed in Plans 01-03)"
      - "Running actual dispatch sweeps (deferred to Phase 3)"
      - "Modifying task definitions (deferred to Phase 2)"
  context_intake:
    must_read_refs: ["ref-handoff"]
    must_include_prior_outputs:
      - "GPD/phases/01-reliability-and-measurement-patches/01-01-SUMMARY.md"
      - "GPD/phases/01-reliability-and-measurement-patches/01-02-SUMMARY.md"
      - "GPD/phases/01-reliability-and-measurement-patches/01-03-SUMMARY.md"
    crucial_inputs:
      - "CI checks: checks (ubuntu-latest) and checks (macos-latest) must pass"
      - "cargo fmt, cargo test, cargo clippy --workspace --all-targets -- -D warnings must all pass"
      - "No #[allow], #[ignore], or test removal relative to pre-patch baseline"
  claims:
    - id: "claim-local-verification"
      statement: "All local verification gates pass on the combined patch set without weakening any existing gate"
      claim_kind: "result"
      deliverables: ["deliv-verification-evidence"]
      acceptance_tests: ["test-cargo-suite", "test-python-suite", "test-no-regressions"]
      references: ["ref-handoff"]
    - id: "claim-ci-evidence"
      statement: "GitHub CI checks pass on both ubuntu-latest and macos-latest for the branch containing all Phase 1 patches"
      claim_kind: "result"
      deliverables: ["deliv-verification-evidence"]
      acceptance_tests: ["test-ci-green"]
      references: ["ref-handoff"]
  deliverables:
    - id: "deliv-verification-evidence"
      kind: "report"
      path: "GPD/phases/01-reliability-and-measurement-patches/01-04-SUMMARY.md"
      description: "Verification evidence: local test results, CI check status, regression audit findings"
      must_contain:
        - "cargo fmt exit status"
        - "cargo test exit status and test count"
        - "cargo clippy exit status"
        - "Python fixture test results"
        - "Regression audit: no tests removed or suppressed"
        - "CI check status with branch and commit hash"
  references:
    - id: "ref-handoff"
      kind: "prior_artifact"
      locator: "HANDOFF_TO_GPD.md"
      role: "must_consider"
      why_it_matters: "Defines CI requirements (checks on ubuntu-latest and macos-latest), lint gates (-D warnings), and test requirements"
      applies_to: ["claim-local-verification", "claim-ci-evidence"]
      must_surface: true
      required_actions: ["read", "use"]
  acceptance_tests:
    - id: "test-cargo-suite"
      subject: "claim-local-verification"
      kind: "benchmark"
      procedure: "Run cargo fmt --check, cargo test, and cargo clippy --workspace --all-targets -- -D warnings in sequence"
      pass_condition: "All three exit with status 0"
      evidence_required: ["deliv-verification-evidence"]
      automation: "automated"
    - id: "test-python-suite"
      subject: "claim-local-verification"
      kind: "benchmark"
      procedure: "Run python3 -m pytest experiments/test_tally.py scripts/test_dispatch_cost_report.py -v (or equivalent unittest invocation)"
      pass_condition: "All Python fixture tests pass"
      evidence_required: ["deliv-verification-evidence"]
      automation: "automated"
    - id: "test-no-regressions"
      subject: "claim-local-verification"
      kind: "consistency"
      procedure: "Compare pre-patch and post-patch: (1) count #[allow] attributes in changed files, (2) count #[ignore] attributes, (3) count test functions. Verify no net reduction in test count and no new suppressions."
      pass_condition: "Test count >= pre-patch count; no new #[allow(clippy::...)] on changed code (existing too_many_arguments is acceptable); no new #[ignore]"
      evidence_required: ["deliv-verification-evidence"]
      automation: "hybrid"
    - id: "test-ci-green"
      subject: "claim-ci-evidence"
      kind: "benchmark"
      procedure: "Push branch, create PR, wait for CI checks to complete"
      pass_condition: "checks (ubuntu-latest) and checks (macos-latest) both report success. Record: date, branch name, commit hash, check names, check results."
      evidence_required: ["deliv-verification-evidence"]
      automation: "automated"
  forbidden_proxies:
    - id: "fp-weakened-gates"
      subject: "claim-local-verification"
      proxy: "CI passes because a lint warning was suppressed or a failing test was removed"
      reason: "Would hide regressions and violate HANDOFF_TO_GPD.md hard constraints"
    - id: "fp-local-only"
      subject: "claim-ci-evidence"
      proxy: "Claiming verification based on local tests only without CI evidence on both platforms"
      reason: "OS-specific behavior in path handling or process spawning could hide platform-specific bugs"
  links:
    - id: "link-local-to-evidence"
      source: "claim-local-verification"
      target: "deliv-verification-evidence"
      relation: "supports"
      verified_by: ["test-cargo-suite", "test-python-suite", "test-no-regressions"]
    - id: "link-ci-to-evidence"
      source: "claim-ci-evidence"
      target: "deliv-verification-evidence"
      relation: "supports"
      verified_by: ["test-ci-green"]
  uncertainty_markers:
    weakest_anchors:
      - "CI may fail on one platform for OS-specific reasons unrelated to the patches (e.g., test timing, path separators)"
    disconfirming_observations:
      - "Any cargo test failure on the combined patch set"
      - "Any new clippy warning in files modified by Phase 1"
      - "CI fails on one platform while passing on the other"
---

<objective>
Run the complete verification suite on the combined Phase 1 patch set and collect CI evidence proving all patches pass without regressions.

Purpose: VAL-01 requires that all reliability patches pass verification gates without weakening existing constraints. This is the final gate before Phase 1 is complete.
Output: Verification evidence summary with local test results, CI check status, and regression audit.
</objective>

<context>
@GPD/ROADMAP.md
@GPD/STATE.md
@HANDOFF_TO_GPD.md
@GPD/phases/01-reliability-and-measurement-patches/01-01-SUMMARY.md
@GPD/phases/01-reliability-and-measurement-patches/01-02-SUMMARY.md
@GPD/phases/01-reliability-and-measurement-patches/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run local verification suite and regression audit</name>
  <files></files>
  <action>
  1. Run the full local verification suite on the current branch with all Phase 1 patches:
     ```
     cargo fmt --check
     cargo test
     cargo clippy --workspace --all-targets -- -D warnings
     ```
     Record exit status of each command.

  2. Run Python fixture tests:
     ```
     python3 -m pytest experiments/test_tally.py -v 2>&1 || python3 -m unittest experiments.test_tally -v 2>&1
     python3 -m pytest scripts/test_dispatch_cost_report.py -v 2>&1 || python3 -m unittest scripts.test_dispatch_cost_report -v 2>&1
     ```
     Record exit status and test count.

  3. Regression audit:
     - Count #[allow(clippy::...)] in src/dispatch.rs, src/main.rs, src/mcp_server.rs. Compare with pre-patch count (expected: only the existing too_many_arguments on apply_result).
     - Count #[ignore] attributes in test code. Should not increase.
     - Count #[test] functions in src/dispatch.rs::tests. Should increase (new tests added).
     - Verify no test functions were removed by comparing test names with the pre-patch list.

  4. If any check fails: diagnose, fix, and re-run. Do NOT suppress or weaken any gate.

  5. Record all evidence in a structured format for the SUMMARY.
  </action>
  <verify>
  - cargo fmt --check exits 0
  - cargo test exits 0 with all tests passing
  - cargo clippy exits 0 with no warnings
  - Python fixture tests all pass
  - Regression audit: no new suppressions, no removed tests
  </verify>
  <done>Local verification suite passes clean with documented evidence</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Review verification evidence and approve for CI submission</name>
  <files></files>
  <action>
  Present the following for human review:
  1. Full local verification suite results (cargo fmt, test, clippy, Python tests)
  2. Regression audit showing no weakened gates
  3. Test count showing net increase (new tests added, none removed)

  Verify checklist:
  - Review the cargo test output: all tests green?
  - Review the clippy output: zero warnings?
  - Review the regression audit: #[allow] and #[ignore] counts unchanged?
  - Review Python fixture test output: all passing?
  - Decision: proceed to push branch and create PR for CI validation?
  </action>
  <verify>Human confirms local verification evidence is clean and approves CI submission</verify>
  <done>Human approves pushing branch and creating PR [Y/n/e] (Enter = Y)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify CI results and collect evidence</name>
  <files></files>
  <action>
  Present the following for human review:
  Branch pushed and PR created. CI checks running (or completed) on ubuntu-latest and macos-latest.

  Verify checklist:
  - Check CI status: both checks (ubuntu-latest) and checks (macos-latest) green?
  - Record: date, branch name, commit hash, CI check names, pass/fail status
  - If CI fails: diagnose the failure, fix, re-push, and re-check
  - Confirm: the branch is ready for merge review
  </action>
  <verify>Human confirms CI checks pass on both platforms and evidence is recorded</verify>
  <done>Human approves CI evidence and confirms branch is ready for merge [Y/n/e] (Enter = Y)</done>
</task>

</tasks>

<verification>
- Local: cargo fmt, cargo test, cargo clippy all pass
- Local: Python fixture tests all pass
- Audit: no regressions in test coverage or lint gates
- CI: both platform checks pass
- Evidence: all results recorded with timestamps and commit hashes
</verification>

<success_criteria>
- cargo fmt --check exits 0
- cargo test exits 0 (N tests pass, where N >= pre-patch count + new tests)
- cargo clippy --workspace --all-targets -- -D warnings exits 0
- Python fixture tests pass
- CI checks (ubuntu-latest) and checks (macos-latest) pass
- No tests removed, no lint gates weakened
- Evidence recorded in SUMMARY with date, branch, commit hash
</success_criteria>

<output>
After completion, create `GPD/phases/01-reliability-and-measurement-patches/01-04-SUMMARY.md`.
</output>
